<template>
   <NavComponent />
</template>

<script setup lang="ts">
   import NavComponent from '../components/NavComponent.vue';
   //  import { ref, onMounted } from 'vue';
   //  import axios from 'axios';

   //  const countriesList = ref(new Set());

   //  const watchlist = async () => (await axios.get(process.env.baseURL + 'countries')).data;

   //  onMounted(async () => {
   //     for (const i of await watchlist()) {
   //        countriesList.value.add(i);
   //     }
   //  });
</script>
